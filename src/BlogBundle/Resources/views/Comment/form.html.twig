{# src/BlogBundle/Resources/views/Comment/form.html.twig #}

<form action="{{ path('blog_comment_create', { 'article_id' : comment.article.id } ) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <input type="submit" value="Submit">
</form>