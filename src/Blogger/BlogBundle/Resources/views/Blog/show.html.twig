{# src/Blogger/BlogBundle/Resouces/views/Blog/show.html.twig #}
{% extends 'BlogBundle::blog.html.twig' %}

{% block title %}{{ blog.title }}{% endblock %}

{% block body %}
    <article class="blog">
        <header>
            <div class="date"><time datetime="{{ blog.created|date('c') }}">{{ blog.created|date('l, F j, Y') }}</time></div>
            <h2>{{ blog.title }}</h2>
        </header>
        <img src="{{ asset(['images/', blog.image]|join) }}" alt="{{ blog.title }} No se ha encontrado la imagen" class="large" />
        <div>
            <p>{{ blog.blog }}</p>
        </div>
    </article>
    <section class="comments" id="comments">
        <section class="previous-comments">
            <h3>Comentarios</h3>
            {% include 'BlogBundle:Comment:index.html.twig' with { 'comments': comments } %}
        </section>
        <h3>Añadir comentario</h3>
        {% render controller('BlogBundle:Comment:new' , { 'blog_id': blog.id }) %}
    </section>
{% endblock %}